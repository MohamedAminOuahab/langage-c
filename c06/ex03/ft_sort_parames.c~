#include <unistd.h>

void	ft_putchar(char c)
{
	write(1, &c, 1);
}

void	ft_putstr(char	 **str)
{
	int	i;
	int	j;

	i =	1;
	while(str[i])
	{
		j =	0;
		while(str[i][j])
		{
			write(1, &str[i][j], 1);
			j++;
		}
		i++;
		ft_putchar('\n');
	}
}

int	main(int	ac, char	**av)
{
	int i;
	int j;
	char *tmp;

	i = 1;
	while(i < ac)
	{
		j = 1;
		while(j < ac)
		{
			if(av[i] > av[j])
			{
				tmp = av[i];
				av[i] = av[j];
				av[j] = tmp;
			}
			j++;
		}
		i++;
	}
	ft_putstr(av);

	
}
